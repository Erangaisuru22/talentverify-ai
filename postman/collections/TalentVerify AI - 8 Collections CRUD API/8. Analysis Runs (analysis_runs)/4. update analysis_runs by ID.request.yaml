$kind: http-request
url: "{{baseUrl}}/api/crud/analysis_runs/{{runId}}"
method: PUT
headers:
  Content-Type: application/json
  Authorization: Bearer {{token}}
body:
  type: text
  content: |-
    {
      "score": 90.0,
      "notes": "Score updated after manual skills validation."
    }
order: 4000
