$kind: http-request
name: "6. deleteAll cv_documents (Safety: confirm=true)"
url: "{{baseUrl}}/api/crud/cv_documents?confirm=true&parsing_status=failed"
method: DELETE
headers:
  Authorization: Bearer {{token}}
queryParams:
  confirm: "true"
order: 6000
