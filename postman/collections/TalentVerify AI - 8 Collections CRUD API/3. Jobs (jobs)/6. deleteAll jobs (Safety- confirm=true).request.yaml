$kind: http-request
name: "6. deleteAll jobs (Safety: confirm=true)"
url: "{{baseUrl}}/api/crud/jobs?confirm=true&status=draft"
method: DELETE
headers:
  Authorization: Bearer {{token}}
queryParams:
  confirm: "true"
order: 6000
